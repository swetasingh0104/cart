import React from "react";

const Total = (props) => {
    return (
        <div  className="total">
           <div className="pos"> Total:{props.count}</div>
        </div>
    );
}



export default Total